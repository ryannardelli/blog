<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
<link rel="stylesheet" href="/css/dashboard/profile.css">

{{!--

<body>
    <div class="row mt-4">
        <div class="col-md-12">
            <div class="profile-card bg-white shadow">
                <div class="text-center">
                    <img src="{{user.profile_picture}}" alt="Foto de perfil" class="img-fluid">
                    <h3>{{user.firstName}} {{user.lastName}}</h3>
                    <p class="text-muted">{{user.function}}</p>
                </div>

                <div class="list-group mt-4">
                    <div class="list-group-item">
                        <strong>Nome:</strong> {{user.name}}
                    </div>
                    <div class="list-group-item">
                        <strong>Função:</strong> {{user.role}}
                    </div>
                    <div class="list-group-item">
                        <strong>Email:</strong> {{user.email}}
                    </div>

                    <div class="list-group-item">
                        <strong>Bio:</strong> {{user.bio}}
                    </div>
                    <div class="list-group-item">
                        <strong>Criado em:</strong> {{formattedDate}}
                    </div>
                </div>
            </div>
        </div>
    </div>
    </div>
</body> --}}

<body>
    <div class="container-profile">
    <div class="profile-card">
        <div class="image">
            <img src="{{user.profile_picture}}" alt="Imagem de Perfil" class="profile-img">
        </div>

        <div class="text-data">
            <span class="name">{{user.firstName}} {{user.lastName}}</span>
            <span class="role">{{user.role}}</span>
        </div>

        <div class="media-buttons">
            <a href="#" class="link" style="background: #e1306c;">
                <i class="bi bi-instagram"></i>
            </a>

            <a href="#" class="link">
                <i class="bi bi-linkedin"></i>
            </a>

            <a href="#" style="background: black;"  class="link">
                <i class="bi bi-github"></i>
            </a>

            <a href="#" style="background: #ff0000;" class="link">
                <i class="bi bi-youtube"></i>
            </a>
        </div>

        <div class="readMore">
            <a href="#" class="button">Ver mais</a>
        </div>

         <div class="analytics">
        <div class="data">
            <i class="bi bi-suit-heart"></i>
            <span class="number">60</span>
        </div>

        <div class="data">
            <i class="bi bi-chat"></i>
            <span class="number">34</span>
        </div>
        
        <div class="data">
            <i class="bi bi-eye"></i>
            <span class="number">12</span>
        </div>
    </div>
    </div>
    </div>
</body>